server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DianaCT15/BC-Config-data.git
          default-label: main
          searchPaths: /ConfigData
  application:
    name: config-server